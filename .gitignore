.vscode/
.emmyrc.json
*.blend1
release.zip
/tmp/

# Python tool scratch
__pycache__/
*.pyc
tools/pz-anim-forge/work/
tools/pz-anim-forge/vendor/

# Java patch: machine-specific install record (dist/ IS committed - see java/README.md)
java/installed-manifest.json

# Decompiled game sources are The Indie Stone's copyright - kept locally to rebuild the patch, never
# published. Only the prebuilt dist/ .class shadow files ship.
java/src/

# packaged distribution archives
/dist/

# Local-only dev files, never published. Drop a local hook directly in this mod named *.local.lua and
# Project Zomboid still loads it in-game, while git will not track it.
*.local.lua
/dev-local/
